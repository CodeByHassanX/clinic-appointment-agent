version: '3.1'

services:
  flowise:
    image: flowiseai/flowise:3.1.3
    container_name: clinic_flowise
    restart: always
    environment:
      - PORT=3000
    ports:
      - "3001:3000"
    volumes:
      - ./flowise_data:/root/.flowise
